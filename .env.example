# Copy to .env and fill in. .env is git-ignored — never commit real keys.
# All LLM usage routes through TokenMart (OpenAI-compatible).
TOKENMART_API_KEY=tm_xxx
TOKENMART_BASE_URL=https://<your-tokenmart-host>/v1
TOKENMART_MODEL=gpt-4o-mini

# Optional: automated competitor search
SERPAPI_API_KEY=
